# LIMS 设备数据对接启动与使用手册

本手册用于在本机启动服务，并验证 LIMS、SCADA 和检测客户端接口。当前录屏和截图使用 Mock 适配器，只验证协议、状态和时间记录，不生成真实媒体文件。

## 1. 环境要求

- Windows、Linux 或 macOS
- Python 3.10 或更高版本
- 可用端口 8000

## 2. 安装

在项目根目录执行：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements-dev.txt
```

如果 PowerShell 禁止激活脚本，也可以直接使用虚拟环境中的 Python：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

## 3. 启动服务

```powershell
python -m uvicorn main:app --reload
```

正式联调时不要使用 `--reload`：

```powershell
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

检查服务：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

预期结果：

```json
{"status":"ok"}
```

接口页面：

- Swagger：<http://127.0.0.1:8000/docs>
- OpenAPI JSON：<http://127.0.0.1:8000/openapi.json>

## 4. 检测客户端联调

### 4.1 开始检测

```powershell
$body = @{ blindSampleNo = "92fbbc8e88c949158fade880b8d69b27" } | ConvertTo-Json
Invoke-RestMethod `
  -Method Post `
  -Uri http://127.0.0.1:8000/startTest `
  -ContentType "application/json" `
  -Body $body
```

预期结果：

```json
{
  "code": "0010",
  "response": null,
  "message": "成功"
}
```

服务会创建一个 `recording` 状态的检测会话，并记录 UTC 开始时间。

### 4.2 结束检测

```powershell
$body = @{ blindSampleNo = "92fbbc8e88c949158fade880b8d69b27" } | ConvertTo-Json
Invoke-RestMethod `
  -Method Post `
  -Uri http://127.0.0.1:8000/stopTest `
  -ContentType "application/json" `
  -Body $body
```

预期结果同样为 `code = "0010"`。服务会调用停止采集适配器，记录结束时间，并将会话更新为 `stopped`。

### 4.3 异常和幂等行为

- `blindSampleNo` 缺失、为空或请求中存在额外字段：HTTP 422。
- 没有开始过检测就调用 `/stopTest`：HTTP 200，`code = "500"`。
- 对同一活动会话重复调用 `/startTest`：HTTP 200，不重复启动采集。
- 对已经结束的会话重复调用 `/stopTest`：HTTP 200，不重复截图。
- 采集适配器失败：HTTP 200，`code = "500"`，不会错误地把会话标记为成功结束。

## 5. 完整工单闭环

### 5.1 LIMS 下发工单

```powershell
$body = @{
  order_no = "WO20260917001"
  experimenter_id = "EMP001"
  project_name = "钢材成分检测"
  samples = @(@{ sample_no = "S001"; sample_name = "钢材样品1" })
  test_items = @("Fe", "C", "Mn")
} | ConvertTo-Json -Depth 5
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/api/work-orders `
  -ContentType "application/json" -Body $body
```

### 5.2 SCADA 获取工单

```powershell
Invoke-RestMethod `
  "http://127.0.0.1:8000/api/scada/work-orders?experimenter_id=EMP001"
```

### 5.3 SCADA 上传结果

```powershell
$body = @{
  order_no = "WO20260917001"
  results = @(
    @{ sample_no = "S001"; test_item = "Fe"; value = 96.2; unit = "%" },
    @{ sample_no = "S001"; test_item = "C"; value = 0.42; unit = "%" }
  )
  finished = $true
} | ConvertTo-Json -Depth 5
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/api/scada/results `
  -ContentType "application/json" -Body $body
```

### 5.4 查询和回推

```powershell
Invoke-RestMethod `
  "http://127.0.0.1:8000/api/work-orders/WO20260917001/results"

Invoke-RestMethod -Method Post `
  "http://127.0.0.1:8000/api/work-orders/WO20260917001/push-to-lims"
```

默认回推到本地 Mock LIMS，服务端控制台会打印收到的 JSON。

## 6. 配置正式 LIMS

PowerShell 临时配置：

```powershell
$env:LIMS_RESULT_URL = "https://lims.example.com/api/results"
$env:CAPTURE_BACKEND = "mock"
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

若需要更换数据库路径：

```powershell
$env:LIMS_DB_PATH = "D:\data\lims_demo.db"
```

修改环境变量后需要重启服务。

## 7. 数据和重置

数据库包含以下主要表：

- `work_orders`：工单主表
- `samples`：工单样品
- `test_results`：检测结果
- `detection_sessions`：检测客户端会话和采集状态

开发阶段如需重新测试，可在确认数据无需保留后删除 `lims_demo.db` 再启动服务。删除数据库会清空本地工单、结果和检测会话。

## 8. 测试与排查

```powershell
python -m pytest
python -m compileall app main.py
python -m pip check
```

常见问题：

- 端口占用：使用 `--port 8001`，并同步修改测试地址。
- 结果上传返回 400：检查样品号是否属于工单。
- 回推返回 502：检查 `LIMS_RESULT_URL`、网络和对方响应状态。
- 看到 Mock 适配器成功但没有视频：这是当前预期行为，必须接入真实 `CaptureAdapter` 才会产生媒体文件。

## 9. 正式联调前确认

- 对方是否要求 HTTP 200 搭配响应体 `code`，当前实现按接口文档处理。
- 盲样号是否与 `sample_no` 使用同一编码规则。
- 正式录屏程序、截图程序、文件目录和运行账户权限。
- LIMS 鉴权方式、HTTPS 证书、超时、重试和幂等规则。
- 数据库备份、访问控制、日志脱敏和磁盘容量告警。
