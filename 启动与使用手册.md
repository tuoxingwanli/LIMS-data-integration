# LIMS / SCADA 数据对接启动与使用手册

## 1. 环境要求

- Windows、Linux 或 macOS
- Python 3.10 及以上版本
- 可用端口：默认 `8000`

以下命令默认在本项目根目录执行。Windows PowerShell 可直接使用文中的命令。

## 2. 首次安装

建议使用项目专用虚拟环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

如果 PowerShell 禁止执行激活脚本，可不激活，后续直接使用 `.\.venv\Scripts\python.exe`。

## 3. 启动服务

开发联调模式：

```powershell
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

看到 `Application startup complete` 即表示启动成功。首次启动会在根目录自动创建 `lims_demo.db`。

常用入口：

- Swagger 接口页：<http://127.0.0.1:8000/docs>
- OpenAPI JSON：<http://127.0.0.1:8000/openapi.json>
- 健康检查：<http://127.0.0.1:8000/health>

停止服务：在运行服务的终端按 `Ctrl+C`。

## 4. 完整闭环测试

另开一个 PowerShell 窗口，依次执行下面四步。

### 4.1 模拟 LIMS 下发工单

```powershell
$workOrder = @{
    order_no = "WO20260910001"
    experimenter_id = "EMP001"
    project_name = "钢材成分检测"
    samples = @(
        @{ sample_no = "S001"; sample_name = "钢材样品1" }
        @{ sample_no = "S002"; sample_name = "钢材样品2" }
    )
    test_items = @("Fe", "C", "Mn")
} | ConvertTo-Json -Depth 5

Invoke-RestMethod `
    -Method Post `
    -Uri "http://127.0.0.1:8000/api/work-orders" `
    -ContentType "application/json; charset=utf-8" `
    -Body ([Text.Encoding]::UTF8.GetBytes($workOrder))
```

预期：`success=true`、`duplicate=false`。重复执行时返回 `duplicate=true`，不会新增重复工单。

### 4.2 模拟 SCADA 获取工单

```powershell
Invoke-RestMethod `
    -Method Get `
    -Uri "http://127.0.0.1:8000/api/scada/work-orders?experimenter_id=EMP001" |
    ConvertTo-Json -Depth 8
```

预期：返回刚创建的工单、样品和检测项目，工单状态为 `pending`。

### 4.3 模拟 SCADA 上传结果

```powershell
$result = @{
    order_no = "WO20260910001"
    results = @(
        @{ sample_no = "S001"; test_item = "Fe"; value = 96.2; unit = "%" }
        @{ sample_no = "S001"; test_item = "C"; value = 0.42; unit = "%" }
        @{ sample_no = "S001"; test_item = "Mn"; value = 1.13; unit = "%" }
        @{ sample_no = "S002"; test_item = "Fe"; value = 95.8; unit = "%" }
    )
    finished = $true
} | ConvertTo-Json -Depth 5

Invoke-RestMethod `
    -Method Post `
    -Uri "http://127.0.0.1:8000/api/scada/results" `
    -ContentType "application/json; charset=utf-8" `
    -Body ([Text.Encoding]::UTF8.GetBytes($result))
```

`finished=true` 表示整张工单完成，状态变为 `completed`；分批上传时应先传 `false`，最后一次再传 `true`。

查询已保存结果：

```powershell
Invoke-RestMethod `
    -Method Get `
    -Uri "http://127.0.0.1:8000/api/work-orders/WO20260910001/results" |
    ConvertTo-Json -Depth 8
```

### 4.4 将结果回推 LIMS

```powershell
Invoke-RestMethod `
    -Method Post `
    -Uri "http://127.0.0.1:8000/api/work-orders/WO20260910001/push-to-lims"
```

默认配置会把结果发送给同一服务内的 Mock LIMS。预期接口返回成功，服务终端打印收到的 JSON，工单状态变为 `pushed`，结果的 `pushed_to_lims` 变为 `1`。

## 5. 正式 LIMS 地址配置

在启动服务前设置环境变量：

```powershell
$env:LIMS_RESULT_URL = "https://lims.example.com/api/results"
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

也可指定数据库位置：

```powershell
$env:LIMS_DB_PATH = "D:\lims-data\lims_demo.db"
```

环境变量只对当前 PowerShell 会话及其启动的进程生效。正式地址、鉴权方式、字段名和成功响应格式必须与第三方确认后再配置或改造。

## 6. 常见响应与排查

| HTTP 状态 | 含义 | 处理建议 |
| --- | --- | --- |
| `200` | 请求处理成功 | 检查响应中的 `success`、`duplicate` 和状态字段 |
| `400` | 业务条件不满足 | 检查工单是否完成、样品是否属于工单、是否已有结果 |
| `404` | 工单不存在 | 先确认 `order_no`，再确认 LIMS 下发是否成功 |
| `422` | JSON 或参数校验失败 | 按 `/docs` 中模型检查必填字段和字段类型 |
| `502` | 调用第三方 LIMS 失败 | 检查 `LIMS_RESULT_URL`、网络、证书及对方服务状态；可原请求重试 |

其他常见问题：

- 端口占用：启动时改用 `--port 8001`，同时将本地 Mock 地址改成对应端口。
- 想重新测试：停止服务后删除 `lims_demo.db`，再启动即可创建空数据库。删除会清空全部本地工单和结果，请先确认数据无需保留。
- 正式联调不要使用 `--reload`；它仅适合开发阶段。
- 修改 `LIMS_RESULT_URL` 后需要重启服务才会生效。

## 7. 正式对接前确认清单

- 工单号是否全局唯一。
- 实验人员唯一 ID 的正式字段名。
- 样品和检测项目的正式 JSON 结构及数量限制。
- SCADA 的轮询周期、超时和重复上传规则。
- LIMS 结果接收 URL、鉴权方式、成功响应和错误码。
- LIMS 是否按 `X-Idempotency-Key` 实现幂等。
- 网络区域、防火墙、HTTPS 证书和时间同步要求。
