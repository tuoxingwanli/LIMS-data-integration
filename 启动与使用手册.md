# LIMS / SCADA 设备数据对接启动与使用手册

本手册说明如何启动服务、使用网页联调控制台，并用命令行验证 LIMS、SCADA 和检测客户端接口。当前录屏和截图使用 Mock 适配器，只验证协议、状态和时间记录，不生成真实媒体文件。

## 1. 环境要求

- Windows、Linux 或 macOS
- Python 3.10 及以上
- 8000 端口可用

## 2. 安装依赖

在项目根目录执行：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements-dev.txt
```

如果 PowerShell 不允许激活脚本，也可以直接使用虚拟环境中的 Python：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
```

## 3. 启动服务

开发联调：

```powershell
python -m uvicorn main:app --reload
```

正式联调不使用热重载：

```powershell
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

访问地址：

- 网页联调控制台：<http://127.0.0.1:8000/>
- Swagger：<http://127.0.0.1:8000/docs>
- OpenAPI：<http://127.0.0.1:8000/openapi.json>

检查服务：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

预期结果：

```json
{"status":"ok"}
```

## 4. 网页联调控制台

根路径由 FastAPI 的 `StaticFiles` 直接托管，网页不需要单独启动 Node.js 或其他前端服务。

推荐演示顺序：

1. 确认顶部“服务在线”和“Mock”状态。
2. 创建一张 LIMS 工单，填写工单号、实验人员、样品和检测项目。
3. 查询 SCADA 工单，确认工单已经进入待处理列表。
4. 输入盲样号，点击“开始检测”，再点击“结束检测”。
5. 上传一条实验结果，点击“查询结果”确认状态。
6. 点击“回推 LIMS”，在最近响应和操作记录中查看结果。

页面右侧会保留最近一次 API 响应，底部会记录接口路径、HTTP 状态和时间。接口业务失败时，即使 HTTP 为 200，响应区仍会标红并显示 `code = "500"`。复制按钮只复制当前 JSON，不会修改服务端数据。

## 5. 检测客户端接口

### 5.1 开始检测

```powershell
$body = @{ blindSampleNo = "92fbbc8e88c949158fade880b8d69b27" } | ConvertTo-Json
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/startTest `
  -ContentType "application/json" -Body $body
```

服务创建 `recording` 状态的检测会话，并记录 UTC 开始时间。盲样号可以独立创建会话；如果能匹配已有 `samples.sample_no`，会保存样品关联。

### 5.2 结束检测

```powershell
$body = @{ blindSampleNo = "92fbbc8e88c949158fade880b8d69b27" } | ConvertTo-Json
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/stopTest `
  -ContentType "application/json" -Body $body
```

采集适配器成功后，会记录 UTC 结束时间并将会话改为 `stopped`。重复结束已经停止的会话仍返回成功；没有对应会话时返回 `code = "500"`。结束采集失败时会保留 `recording` 状态，并把失败原因写入会话记录。

### 5.3 响应和异常

成功：

```json
{"code":"0010","response":null,"message":"成功"}
```

业务失败仍使用 HTTP 200：

```json
{"code":"500","response":null,"message":"未找到正在进行的检测"}
```

缺少 `blindSampleNo`、空字符串或额外字段会返回 HTTP 422。重复开始不会重复创建会话或重复启动采集。采集失败会返回 `code = "500"`，并保留失败原因。

## 6. 完整工单闭环

### 6.1 LIMS 下发工单

```powershell
$body = @{
  order_no = "WO-DEMO-001"
  experimenter_id = "EMP001"
  project_name = "设备联调演示"
  samples = @(@{ sample_no = "S-DEMO-001"; sample_name = "联调样品" })
  test_items = @("Fe", "C", "Mn")
} | ConvertTo-Json -Depth 5
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/api/work-orders `
  -ContentType "application/json" -Body $body
```

相同 `order_no` 再次提交时返回 `duplicate = true`，不会创建重复工单。

### 6.2 SCADA 查询工单

```powershell
Invoke-RestMethod `
  "http://127.0.0.1:8000/api/scada/work-orders?experimenter_id=EMP001"
```

### 6.3 上传实验结果

```powershell
$body = @{
  order_no = "WO-DEMO-001"
  results = @(
    @{ sample_no = "S-DEMO-001"; test_item = "Fe"; value = 96.2; unit = "%" },
    @{ sample_no = "S-DEMO-001"; test_item = "C"; value = 0.42; unit = "%" }
  )
  finished = $true
} | ConvertTo-Json -Depth 5
Invoke-RestMethod -Method Post `
  -Uri http://127.0.0.1:8000/api/scada/results `
  -ContentType "application/json" -Body $body
```

结果以 `(work_order_id, sample_no, test_item)` 作为 UPSERT 键。`finished = false` 时工单为 `in_progress`，`true` 时为 `completed`。

### 6.4 查询和回推

```powershell
Invoke-RestMethod `
  "http://127.0.0.1:8000/api/work-orders/WO-DEMO-001/results"

Invoke-RestMethod -Method Post `
  "http://127.0.0.1:8000/api/work-orders/WO-DEMO-001/push-to-lims"
```

默认回推到本地 Mock LIMS。服务会携带 `X-Idempotency-Key: WO-DEMO-001`，只有对方 HTTP 请求成功后才把本地结果标记为已回推。

## 7. 配置正式 LIMS

PowerShell 临时配置：

```powershell
$env:LIMS_RESULT_URL = "https://lims.example.com/api/results"
$env:CAPTURE_BACKEND = "mock"
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

更换数据库路径：

```powershell
$env:LIMS_DB_PATH = "D:\data\lims_demo.db"
```

修改环境变量后需要重启服务。正式地址、鉴权方式、HTTPS 证书、超时和重试规则应以双方确认结果为准。

## 8. 数据库与采集限制

SQLite 包含 `work_orders`、`samples`、`test_results` 和 `detection_sessions` 四张主要表。初始化采用 `CREATE TABLE IF NOT EXISTS`，不会覆盖已有数据。

`CAPTURE_BACKEND=mock` 只返回成功状态，不创建视频、截图或文件引用。正式实现需要在 `app/integrations/capture.py` 中替换 `CaptureAdapter`，补充进程、文件落盘、权限和失败处理。

## 9. 测试与排查

```powershell
pytest
python -m compileall app main.py
python -m pip check
```

常见问题：

- 端口占用：使用 `--port 8001`，同时修改访问地址。
- 页面打不开：确认服务已经启动，并访问 `/health`。
- 结果上传失败：检查工单号和样品号是否属于同一工单。
- 回推失败：检查 `LIMS_RESULT_URL`、网络和对方响应。
- 看到 Mock 成功但没有视频：这是预期行为，需要接入真实采集适配器。
