# LIMS 设备数据对接项目架构分析

## 1. 项目目标

本项目是一个用于接口联调的 LIMS/SCADA 数据对接服务。它把 LIMS 工单、SCADA 实验结果、检测客户端的开始/结束通知和 LIMS 结果回推放在同一个可运行闭环中。

当前重点是验证字段、状态、幂等和错误语义，不把尚未确认的鉴权、真实录屏工具或生产级消息系统写死在原型中。

## 2. 业务边界

服务负责：

- 接收和保存 LIMS 工单及样品。
- 向 SCADA 提供待处理工单。
- 接收、校验和保存实验结果。
- 接收检测客户端的 `/startTest`、`/stopTest` 通知。
- 记录检测会话时间和采集适配器结果。
- 将完成结果通过 HTTP 回推 LIMS。

服务暂不负责：

- 实际 Windows 录屏和截图程序。
- 用户认证、权限管理和 HTTPS 终止。
- 跨实例任务调度、消息队列和自动重试。
- LIMS 正式字段映射以外的业务审批。

## 3. 总体数据流

```text
LIMS
  | POST /api/work-orders
  v
本服务 SQLite
  | GET /api/scada/work-orders
  v
SCADA
  | POST /startTest
  | POST /api/scada/results
  | POST /stopTest
  v
检测会话 + 实验结果
  | POST /api/work-orders/{order_no}/push-to-lims
  v
LIMS 或本地 Mock LIMS
```

## 4. 代码分层

```text
main.py
  -> app.main.create_app
      -> api.routes       接收 HTTP 请求和返回响应
      -> schemas          校验输入和约束响应结构
      -> services         编排业务规则和事务
      -> repositories     执行 SQLite 查询
      -> integrations     对接 LIMS、录屏和截图实现
      -> db               连接管理和增量建表
```

根目录 `main.py` 只导出 `app`，因此保留原有 Uvicorn 启动命令；具体业务不再堆放在单个 Python 文件中。

## 5. 数据模型

### 5.1 工单主表 work_orders

关键字段为 `order_no`、`experimenter_id`、`project_name`、`status` 和 `raw_json`。`order_no` 唯一，用于接收幂等。

工单状态为：

```text
pending -> in_progress -> completed -> pushed
```

### 5.2 样品表 samples

样品属于某张工单，`(work_order_id, sample_no)` 唯一。上传结果前会检查样品是否属于目标工单。

### 5.3 结果表 test_results

结果以 `(work_order_id, sample_no, test_item)` 唯一。重复上传会更新值和单位，并把 `pushed_to_lims` 重置为 0，避免新值被误认为已经回推。

### 5.4 检测会话表 detection_sessions

检测会话保存：

| 字段 | 说明 |
| --- | --- |
| `blind_sample_no` | 接口传入的盲样号 |
| `sample_id` | 可选匹配的现有样品 ID |
| `status` | `recording`、`stopped` 或 `failed` |
| `started_at` | 服务端 UTC 开始时间 |
| `ended_at` | 服务端 UTC 结束时间 |
| `recording_ref` | 录屏适配器返回的引用 |
| `screenshot_ref` | 截图适配器返回的引用 |
| `capture_mode` | 当前为 `mock` 或测试适配器名称 |
| `error_message` | 采集失败原因 |

盲样号不要求预先存在于 `samples`。如果查询到相同 `sample_no`，会保存可选外键关联；否则会话仍可独立运行。

## 6. 检测客户端接口

### 6.1 开始检测

```text
POST /startTest
Content-Type: application/json
```

请求：

```json
{
  "blindSampleNo": "92fbbc8e88c949158fade880b8d69b27"
}
```

处理逻辑：

1. 校验 `blindSampleNo` 非空且没有额外字段。
2. 如果已有活动会话，直接按幂等成功返回。
3. 调用 `CaptureAdapter.start`。
4. 采集成功后创建 `recording` 会话并记录 UTC 开始时间。

### 6.2 结束检测

```text
POST /stopTest
Content-Type: application/json
```

请求格式与 `/startTest` 相同。服务找到活动会话后调用 `CaptureAdapter.stop`，成功时记录结束时间和截图引用，并更新为 `stopped`。

### 6.3 响应语义

成功：

```json
{
  "code": "0010",
  "response": null,
  "message": "成功"
}
```

业务失败：

```json
{
  "code": "500",
  "response": null,
  "message": "未找到正在进行的检测"
}
```

有效业务请求统一返回 HTTP 200，以兼容接口文档中的返回码约定；JSON 缺字段、空字段或包含额外字段时返回 HTTP 422。

## 7. 状态和幂等规则

检测会话状态：

```text
不存在 --startTest--> recording --stopTest--> stopped
                         |
                         +--采集失败--> failed
```

- 活动会话重复开始：不再次调用采集器。
- 已结束会话重复结束：不再次调用采集器。
- 不存在会话直接结束：返回业务失败 `code = "500"`。
- 结束采集失败：保持 `recording`，不写入成功结束时间。
- 开始采集失败：记录 `failed` 会话和错误原因。

工单和结果沿用原型的幂等规则：工单号唯一，结果按三元组 UPSERT，回推请求携带 `X-Idempotency-Key: order_no`。

## 8. 事务和失败窗口

- 工单和样品在同一个 SQLite 事务中写入。
- 一次结果上传中的所有结果在同一个事务中 UPSERT。
- LIMS 网络调用在本地事务之外执行；只有对方 HTTP 成功后才更新本地 `pushed` 状态。
- 如果 LIMS 已成功但本地状态更新失败，存在重复回推窗口，幂等键用于降低重复写入风险。
- Mock 采集适配器没有外部文件副作用，因此适合联调；真实采集器接入后应补充进程、文件和权限失败处理。

## 9. 可替换适配器

`app/integrations/capture.py` 定义 `CaptureAdapter` 协议：

```python
class CaptureAdapter(Protocol):
    mode: str

    def start(self, blind_sample_no: str) -> CaptureResult: ...
    def stop(self, blind_sample_no: str) -> CaptureResult: ...
```

正式实现可以在其中调用经过确认的 Windows 录屏和截图程序，并返回文件路径、对象存储地址或其他引用。当前 `MockCaptureAdapter` 返回成功但不创建文件，数据库中的引用为空。

## 10. 运行和配置

默认启动：

```powershell
python -m uvicorn main:app --reload
```

配置项：

| 环境变量 | 默认值 | 作用 |
| --- | --- | --- |
| `LIMS_DB_PATH` | `lims_demo.db` | SQLite 文件路径 |
| `LIMS_RESULT_URL` | 本地 Mock 地址 | 结果回推目标 |
| `CAPTURE_BACKEND` | `mock` | 采集适配器类型 |

## 11. 安全和运维风险

当前原型没有鉴权，任何能访问端口的客户端都可以调用接口。正式部署至少需要 HTTPS、来源限制、API 鉴权、敏感字段脱敏日志、数据库备份和磁盘空间监控。

SQLite 适合单机低并发联调。多进程、高频结果上传或多个设备同时接入时，应迁移 PostgreSQL，并将 LIMS 回推改成持久化 Outbox 加后台重试。

## 12. 验证策略

测试覆盖：

- 原有工单、结果 UPSERT、查询和 LIMS 回推闭环。
- 新接口成功、重复调用、参数校验、未知会话和采集失败。
- 检测会话状态、开始/结束时间和样品关联。
- 模块编译、依赖检查、OpenAPI 生成和真实 Uvicorn 启动。

执行命令：

```powershell
python -m pytest
python -m compileall app main.py
python -m pip check
```

## 13. 演进路线

1. 完成双方字段、成功码、鉴权、录屏工具和文件目录确认。
2. 接入真实采集适配器，增加进程存活、文件落盘和权限测试。
3. 增加请求 ID、结构化日志、推送记录和人工重试。
4. 迁移 PostgreSQL，加入数据库迁移脚本和 Outbox worker。
5. 根据实际吞吐量增加限流、告警、权限和多实例部署。
