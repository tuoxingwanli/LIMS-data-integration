# LIMS / SCADA 数据对接项目架构分析

## 1. 项目目标

本项目是用于验证 LIMS 与 SCADA 数据交换流程的最小可运行后端。它不包含前端、复杂权限、消息队列或分布式组件，重点是先跑通以下闭环：

```text
第三方 LIMS 下发工单
        ↓
本系统持久化工单及样品
        ↓
SCADA 按实验人员 ID 获取工单
        ↓
SCADA 上传实验结果
        ↓
本系统保存并汇总结果
        ↓
本系统将结果回推第三方 LIMS
```

## 2. 总体架构

项目采用单体分层思路，但按原型要求全部集中在 `main.py` 中：

| 层次 | 技术/代码 | 职责 |
| --- | --- | --- |
| HTTP 接口层 | FastAPI | 接收 JSON、生成 Swagger 文档、返回统一 HTTP 响应 |
| 数据校验层 | Pydantic | 校验必填字段、禁止未知字段、检查重复样品和检测项目 |
| 业务层 | 路由处理函数 | 执行工单幂等、状态流转、结果 UPSERT、LIMS 回推 |
| 数据访问层 | `sqlite3` | 参数化 SQL、事务提交/回滚、三张业务表读写 |
| 外部集成层 | `httpx` | 异步 POST 实验结果到第三方 LIMS |
| 持久化层 | SQLite | 本地保存工单、样品和结果 |

当前文件结构：

```text
.
├── main.py                 # 应用、模型、数据库和全部接口
├── requirements.txt        # 运行依赖
├── 开发要求.md             # 原始需求
├── 项目架构分析.md         # 本文档
├── 启动与使用手册.md       # 安装、启动和联调步骤
└── lims_demo.db            # 首次启动后自动生成，不纳入版本控制
```

## 3. 数据模型

### 3.1 `work_orders` 工单表

保存工单主信息、实验人员唯一 ID、当前状态及 LIMS 原始 JSON。`order_no` 有唯一约束，用于防止 LIMS 重复下发同一工单。

状态流转：

```text
pending → in_progress → completed → pushed
             └────────→ completed
```

- `pending`：已接收，SCADA 尚未回传结果。
- `in_progress`：SCADA 上传了部分结果，`finished=false`。
- `completed`：SCADA 声明实验完成，可以回推 LIMS。
- `pushed`：LIMS 已成功响应，本地推送状态已更新。

### 3.2 `samples` 样品表

通过 `work_order_id` 关联工单。一个工单可以包含多个样品，`(work_order_id, sample_no)` 联合唯一。样品收到至少一条结果后状态更新为 `completed`。

### 3.3 `test_results` 结果表

保存样品检测项目、值、单位、原始 JSON 和推送标记。`(work_order_id, sample_no, test_item)` 联合唯一；SCADA 重复上传同一结果时执行 UPSERT，覆盖旧值并将 `pushed_to_lims` 重置为 `0`。

## 4. 接口边界

| 调用方 | 方法与路径 | 作用 |
| --- | --- | --- |
| LIMS | `POST /api/work-orders` | 下发工单，按 `order_no` 幂等接收 |
| SCADA | `GET /api/scada/work-orders` | 按 `experimenter_id` 获取未完成工单 |
| SCADA | `POST /api/scada/results` | 上传或更新实验结果 |
| 管理/联调 | `GET /api/work-orders/{order_no}/results` | 查询工单状态及结果 |
| 管理/联调 | `POST /api/work-orders/{order_no}/push-to-lims` | 将已完成结果回推 LIMS |
| 运维 | `GET /health` | 检查服务是否可访问 |
| 本地 Mock | `POST /mock/lims/results` | 模拟第三方 LIMS 接收结果 |

回推请求携带 `X-Idempotency-Key: <order_no>`。第三方 LIMS 应按此键实现幂等，避免“对方已写入但响应丢失”时重复落库。

## 5. 一致性与异常处理

- 工单及其样品在同一数据库事务中写入，失败时整体回滚。
- 结果上传先一次性校验所有样品均属于该工单，再执行写入，避免部分结果落库。
- 所有 SQL 值均通过参数绑定传入。
- 只有第三方 LIMS 返回成功 HTTP 状态后，才把工单改为 `pushed`。
- LIMS 网络或 HTTP 错误转换为 `502`，本地数据仍保持可重试状态。
- 输入模型禁止额外字段，并拒绝空编号、重复样品、重复检测项目和空结果列表。

## 6. 配置方式

| 环境变量 | 默认值 | 说明 |
| --- | --- | --- |
| `LIMS_DB_PATH` | 项目根目录下 `lims_demo.db` | SQLite 文件路径 |
| `LIMS_RESULT_URL` | `http://127.0.0.1:8000/mock/lims/results` | 结果回推地址 |

正式对接时只需把 `LIMS_RESULT_URL` 改成第三方地址，并在确认协议后补充鉴权请求头或签名逻辑。

## 7. 当前适用范围与演进方向

该架构适合单机原型验证、小规模联调和字段协议确认。当前刻意未实现用户鉴权、自动重试队列、审计日志、数据库迁移、多实例并发和定时拉单。

进入生产前建议按优先级演进：

1. 与 LIMS/SCADA 确认正式字段、成功响应、鉴权、超时及重试协议。
2. 增加 API Key 或签名校验，并通过 HTTPS 部署。
3. 增加推送尝试次数、最后错误和请求/响应审计记录。
4. 若改为定时拉单，在现有工单接收逻辑前增加调度器，后续三表和 SCADA 流程可继续复用。
5. 数据量或并发增长后迁移到 PostgreSQL，并拆分模块、增加数据库迁移工具。

