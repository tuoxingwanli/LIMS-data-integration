# LIMS / SCADA 数据对接项目架构分析

> 文档定位：说明当前 Demo 已实现的架构、关键约束和接口契约，并给出从原型走向生产的演进边界。文中“已实现”仅指当前仓库代码；“建议”是后续建设项，不应误认为已经上线。

## 1. 背景、目标与范围

实验室信息管理系统（LIMS）负责生成和管理检测工单；现场 SCADA 负责采集仪器或人工录入的实验结果。本项目在两者之间提供一个轻量数据交换服务，解决以下问题：

1. 接收第三方 LIMS 下发的工单并可靠落库。
2. 按实验人员唯一 ID 将其待办工单提供给 SCADA。
3. 接收 SCADA 分批或重复上传的检测结果。
4. 当工单完成后，将汇总结果回推给 LIMS。
5. 在网络重试与重复请求场景中避免重复工单、重复检测项和错误状态流转。

当前版本的范围是单机、单服务、单 SQLite 文件的接口联调原型。明确不在当前范围内的内容包括：前端页面、账号体系、细粒度权限、Redis、消息队列、容器编排、微服务、LIMS 主动轮询、自动重试调度和生产审计平台。

## 2. 业务上下文与数据流

```text
┌───────────────┐       POST /api/work-orders       ┌───────────────────────┐
│ 第三方 LIMS    │ ─────────────────────────────────▶ │ LIMS 数据对接服务       │
│ 工单来源系统   │                                    │ FastAPI + SQLite        │
└───────────────┘                                    │                       │
        ▲                                            │ work_orders           │
        │ POST 结果                                  │ samples               │
        │ X-Idempotency-Key                          │ test_results          │
        │                                            └───────────┬───────────┘
        │                                                        │ GET /api/scada/work-orders
        │                                                        ▼
        │                                               ┌─────────────────┐
        └────────────────────────────────────────────── │ SCADA / 实验现场 │
                   POST /api/scada/results              └─────────────────┘
```

### 2.1 正向工单链路

1. LIMS 调用 `POST /api/work-orders`，提交工单号、实验人员、样品和检测项目。
2. 服务校验 JSON 后，在同一事务内写入 `work_orders` 和 `samples`。
3. 如果相同 `order_no` 已存在，服务按幂等成功返回，不新增工单。
4. SCADA 使用实验人员 ID 调用 `GET /api/scada/work-orders`，仅获取 `pending` 与 `in_progress` 状态的工单。

### 2.2 结果回传链路

1. SCADA 调用 `POST /api/scada/results`，提交一条或多条样品检测结果。
2. 服务先验证工单和所有样品归属，再以 UPSERT 写入结果。
3. 当 `finished=false` 时工单进入 `in_progress`；当 `finished=true` 时进入 `completed`。
4. 操作人员或上层系统调用 `POST /api/work-orders/{order_no}/push-to-lims`。
5. 服务读取该工单全部结果，POST 到配置的 LIMS 地址；仅在对方返回 2xx 后，更新本地结果推送标记和工单状态为 `pushed`。

## 3. 逻辑架构

虽然第一版代码集中在 `main.py`，内部仍按职责分为六个逻辑层：

| 层次 | 当前实现 | 责任 | 依赖方向 |
| --- | --- | --- | --- |
| 接入层 | FastAPI 路由、Swagger | 暴露 REST 接口、解析 HTTP 请求、返回状态码 | 调用校验与业务逻辑 |
| 输入校验层 | Pydantic 模型与验证器 | 类型、必填项、空值、未知字段、重复样品/项目校验 | 不直接访问数据库 |
| 业务编排层 | 路由处理函数 | 幂等判断、状态流转、推送时机、错误语义 | 调用数据层与 HTTP 客户端 |
| 数据访问层 | `sqlite3` 参数化 SQL | 建表、查询、UPSERT、事务提交与回滚 | 仅访问 SQLite |
| 外部适配层 | `httpx.AsyncClient` | 构造 LIMS 回推 JSON、超时和 HTTP 错误转换 | 访问第三方 HTTP 服务 |
| 基础设施层 | SQLite、环境变量、Uvicorn | 数据持久化、配置注入、进程运行 | 被上层使用 |

这种单体结构适合快速确认协议。后续拆分代码时，应优先按上述职责迁移到 `api/`、`schemas/`、`services/`、`repositories/`、`clients/`，而不是为了拆分而拆分为多个网络服务。

## 4. 代码与运行时组成

```text
项目根目录
├── main.py                 应用入口、全部路由、模型和数据库初始化
├── requirements.txt        fastapi、uvicorn、httpx
├── README.md               项目首页与快速说明
├── 项目架构分析.md          本文档
├── 启动与使用手册.md        PowerShell 安装、启动与联调步骤
├── .gitignore              排除数据库、缓存与虚拟环境
└── lims_demo.db            首次运行生成的 SQLite 数据库（不提交）
```

服务启动时，FastAPI 生命周期钩子执行 `init_db()`：创建表、索引和约束；若表已存在则不覆盖已有数据。数据库默认放在项目根目录，亦可通过 `LIMS_DB_PATH` 指定绝对或相对路径。

## 5. 数据架构

### 5.1 实体关系

```text
work_orders (1) ──────────────< (N) samples
     │
     └────────────────────────< (N) test_results

业务关联：test_results.sample_no 必须属于同一 work_order 的 samples
```

`test_results` 为保持原型简洁，使用 `sample_no` 保存样品业务编号，而非额外增加样品外键；应用层会在写入前校验样品归属。若进入生产并需要更强的参照完整性，建议将其替换为 `sample_id` 外键。

### 5.2 `work_orders`：工单主表

| 字段 | 含义 | 约束/用途 |
| --- | --- | --- |
| `id` | 内部主键 | 自增，供表关联使用 |
| `order_no` | LIMS 工单号 | 非空、唯一；工单接收幂等键 |
| `experimenter_id` | 实验人员唯一 ID | 非空；SCADA 工单分发条件 |
| `project_name` | 项目名称 | 可空展示字段 |
| `status` | 工单状态 | 受 CHECK 约束控制 |
| `raw_json` | 原始工单 JSON | 保留初始字段，方便协议追溯 |
| `created_at` / `updated_at` | 创建与更新时间 | SQLite 当前时间 |

### 5.3 `samples`：样品表

| 字段 | 含义 | 约束/用途 |
| --- | --- | --- |
| `id` | 内部主键 | 自增 |
| `work_order_id` | 所属工单 | 外键关联 `work_orders.id` |
| `sample_no` | 样品业务编号 | 与工单构成联合唯一键 |
| `sample_name` | 样品名称 | 可空展示字段 |
| `status` | 样品状态 | `pending` 或 `completed` |

### 5.4 `test_results`：检测结果表

| 字段 | 含义 | 约束/用途 |
| --- | --- | --- |
| `id` | 内部主键 | 自增 |
| `work_order_id` | 所属工单 | 外键关联 `work_orders.id` |
| `sample_no` | 样品编号 | 业务关联到同工单样品 |
| `test_item` | 检测项目 | 与工单、样品构成唯一业务键 |
| `value` | 检测值 | 统一保存为文本，避免原型阶段精度假设 |
| `unit` | 单位 | 可空，例如 `%`、`mg/L` |
| `raw_json` | 原始结果 JSON | 保留上报原文 |
| `pushed_to_lims` | 是否已成功推送 | `0` 未推送、`1` 已推送 |
| `created_at` | 首次记录时间 | 当前版本 UPSERT 不更新该字段 |

### 5.5 索引与性能边界

已建立的索引服务于主要查询路径：

- `order_no` 唯一索引：工单查找及幂等判断。
- `(experimenter_id, status, created_at)`：SCADA 按人员查询待办工单。
- `samples(work_order_id)`：读取工单样品。
- `test_results(work_order_id)`：读取工单全部结果并回推。

SQLite 适合单机、小并发、联调数据量。它采用文件级写锁，多进程高频写入、跨机器部署或大量历史检索时应迁移至 PostgreSQL 等服务型数据库。

## 6. 状态机与业务规则

### 6.1 工单状态机

```text
                    SCADA 上传 finished=false
     ┌─────────────────────────────────────────┐
     │                                         ▼
pending ───────────────────────────────▶ in_progress
   │                                           │
   │ SCADA 上传 finished=true                  │ SCADA 上传 finished=true
   └───────────────────────────────────────────┴────▶ completed
                                                     │
                                                     │ LIMS 返回 2xx
                                                     ▼
                                                   pushed
```

| 状态 | 含义 | 是否会被 SCADA 获取 | 是否允许回推 |
| --- | --- | --- | --- |
| `pending` | 已接收但尚无结果 | 是 | 否 |
| `in_progress` | 已收到部分结果 | 是 | 否 |
| `completed` | SCADA 已声明工单完成 | 否 | 是 |
| `pushed` | LIMS 已确认接收 | 否 | 是，可因网络不确定性重试 |

当前规则以 SCADA 传入的 `finished` 为完成依据，不自动验证“每个样品的每个 `test_items` 均有结果”。这是为了保持对接协议弹性；若业务要求严格完检，应在后续增加结果完整性校验规则。

### 6.2 样品状态规则

每个样品初始为 `pending`。当前样品收到至少一条合法结果即标记为 `completed`。因此它表示“已开始并产生结果”，而非“该样品的所有检测项目均完整”。若需要精确的样品完成度，应按工单检测项目逐项比对后再更新状态。

## 7. 接口契约

### 7.1 接口一览

| 调用方 | 方法与路径 | 请求目的 | 成功结果 |
| --- | --- | --- | --- |
| LIMS | `POST /api/work-orders` | 下发工单 | 保存成功或幂等确认 |
| SCADA | `GET /api/scada/work-orders?experimenter_id=...` | 领取待办工单 | 返回待办/进行中工单列表 |
| SCADA | `POST /api/scada/results` | 上传检测结果 | 保存或覆盖结果，更新工单状态 |
| 联调/上层 | `GET /api/work-orders/{order_no}/results` | 查询结果 | 返回状态和结果明细 |
| 联调/上层 | `POST /api/work-orders/{order_no}/push-to-lims` | 发起结果回推 | LIMS 接收后更新为 `pushed` |
| 运维 | `GET /health` | 存活探针 | `{ "status": "ok" }` |
| 本地联调 | `POST /mock/lims/results` | 模拟外部 LIMS | 打印并确认收到结果 |

### 7.2 工单下发契约

必填字段是 `order_no`、`experimenter_id`、至少一个 `samples`；`test_items` 可为空。服务拒绝未知字段、空字符串、重复 `sample_no` 和重复/空的检测项目。

```json
{
  "order_no": "WO20260910001",
  "experimenter_id": "EMP001",
  "project_name": "钢材成分检测",
  "samples": [
    {"sample_no": "S001", "sample_name": "钢材样品1"}
  ],
  "test_items": ["Fe", "C", "Mn"]
}
```

### 7.3 结果上传契约

必填字段是 `order_no` 和至少一条 `results`。每条结果需要 `sample_no`、`test_item`、`value`；同一次请求内的 `(sample_no, test_item)` 不可重复，且样品必须属于指定工单。

```json
{
  "order_no": "WO20260910001",
  "results": [
    {"sample_no": "S001", "test_item": "Fe", "value": 96.2, "unit": "%"}
  ],
  "finished": false
}
```

`value` 当前可为字符串、整数或浮点数，最终以字符串保存并按原值格式回推。对于浓度、检出限、合格判定、异常标记等领域字段，需要在与 LIMS/SCADA 确认协议后扩展模型。

### 7.4 结果回推契约

回推请求体由服务生成，包含 `order_no`、`experimenter_id` 与该工单全部结果；请求头包含：

```text
X-Idempotency-Key: <order_no>
```

第三方 LIMS 应对该键实现幂等接收。当前客户端超时为 10 秒，任何连接、超时或非 2xx 响应都会使接口返回 `502`，且不会更新本地为已推送。

### 7.5 错误语义

| HTTP 状态 | 当前含义 | 调用方处理建议 |
| --- | --- | --- |
| `200` | 成功，包括重复工单的幂等成功 | 读取 `success`、`duplicate`、`status` 等字段 |
| `400` | 工单未完成、样品不归属、没有可推送结果等业务错误 | 修正业务条件后重试 |
| `404` | 工单号不存在 | 核对工单下发和编号 |
| `422` | 参数或 JSON 校验不通过 | 按 Swagger 模型修正字段 |
| `500` | 本地数据库写入或状态更新失败 | 保留请求并排查本服务日志/磁盘 |
| `502` | 第三方 LIMS 网络或 HTTP 调用失败 | 核对地址、鉴权、网络和对方响应后重试 |

## 8. 一致性、幂等与重试设计

### 8.1 已实现的幂等点

| 场景 | 机制 | 效果 |
| --- | --- | --- |
| LIMS 重复下发同一工单 | `UNIQUE(order_no)` + 预查询 | 不重复创建工单和样品 |
| SCADA 重复上传同一检测项 | 联合唯一键 + SQLite UPSERT | 覆盖最新值，不产生重复结果 |
| 本系统重复回推 LIMS | `X-Idempotency-Key` | 由接收方按工单号去重 |

### 8.2 事务边界

- 工单与全部样品：同一个 SQLite 事务；任意样品写入失败则整张工单回滚。
- SCADA 结果：先校验请求中的所有样品，之后在一个事务中逐条 UPSERT、更新样品和工单状态；数据库异常时回滚。
- LIMS 回推：网络调用不能与本地数据库事务做分布式原子提交。当前采用“对方成功后再写 `pushed`”策略，保证不会把尚未确认的结果误标为已推送。

### 8.3 必须认知的失败窗口

当 LIMS 已保存结果、但响应在网络中丢失时，本服务会收到超时或错误，并维持未推送状态。后续重试会再次发送同一数据；这正是 `X-Idempotency-Key` 必不可少的原因。双方须明确该键的有效期、重复返回格式和冲突处理规则。

## 9. 安全、合规与数据治理

当前 Demo 不包含认证授权，不应直接暴露到公网或生产网络。上线前至少应落实：

1. **传输安全**：反向代理终止 HTTPS，禁用明文外网 HTTP，校验证书。
2. **调用鉴权**：LIMS 与 SCADA 分别使用 API Key、OAuth2 Client Credentials、mTLS 或签名机制之一；密钥通过环境变量或密钥管理服务注入，绝不写入仓库。
3. **最小权限**：分别限制“下发工单、读取工单、上传结果、触发回推”的调用身份。
4. **输入边界**：限制请求体大小、字符串长度、批量样品/结果数量；生产环境增加速率限制和 WAF 策略。
5. **审计留痕**：记录请求关联 ID、调用方、工单号、结果摘要、状态变化、回推次数与错误原因；敏感样品信息按企业规范脱敏。
6. **数据保留**：明确原始 JSON、结果、操作日志和备份的保留期限、访问责任人与清理流程。
7. **数据质量**：与业务确认数值精度、单位字典、检测项目代码、异常值规则、修订流程和结果版本归属。

## 10. 部署、配置与可观测性

### 10.1 当前启动方式

开发联调使用：

```powershell
python -m pip install -r requirements.txt
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

`--reload` 仅用于开发；它会监控文件并重启进程。正式服务应使用进程管理器或服务管理工具运行固定版本，配套反向代理、日志轮转和自动拉起。

### 10.2 配置项

| 环境变量 | 默认值 | 生产要求 |
| --- | --- | --- |
| `LIMS_DB_PATH` | 根目录 `lims_demo.db` | 置于受备份和访问控制保护的数据盘 |
| `LIMS_RESULT_URL` | 本地 Mock 地址 | 改为正式 HTTPS 接口，不写死在代码中 |

后续可增加 `LIMS_TIMEOUT_SECONDS`、鉴权凭据、日志级别、重试策略和环境标识等显式配置。

### 10.3 健康检查与监控建议

`GET /health` 当前仅表明 Web 进程可响应。生产监控应至少分三类：

- **存活性**：进程、端口、HTTP 响应。
- **就绪性**：数据库可连接、必要配置存在、第三方域名可解析（不建议每次健康检查都调用外部 LIMS）。
- **业务性**：待回推工单数、连续推送失败数、推送延迟、422/502 比例、工单在 `in_progress` 停留时间、数据库文件容量与备份结果。

日志中应包含可关联的 `order_no` 和请求追踪 ID；避免完整打印包含敏感信息的原始 JSON。

## 11. 主要风险与处置策略

| 风险 | 当前影响 | 建议处置 |
| --- | --- | --- |
| LIMS 字段/协议尚未最终确认 | 需改动模型和回推字段 | 以正式接口文档为准，建立字段映射和契约测试 |
| LIMS 成功但响应丢失 | 会发生重复回推 | 双方落实幂等键与结果查询/确认机制 |
| SQLite 多写并发 | 可能出现锁等待 | 原型阶段限制单实例；生产迁移 PostgreSQL |
| SCADA 仅上传部分结果却标记完成 | 不完整结果被回推 | 增加“项目-样品完整性”校验和人工复核 |
| 未鉴权接口暴露 | 可被伪造工单或结果 | 内网隔离、HTTPS、服务身份鉴权和访问日志 |
| 无审计与重试记录 | 故障难以定位 | 记录推送任务、请求摘要、错误、重试时间 |
| 数据库单文件损坏或磁盘满 | 影响服务和历史数据 | 定期备份、容量告警、恢复演练 |

## 12. 从原型到生产的演进路线

### 阶段 A：接口联调（当前）

- 使用 Mock LIMS 跑通四段业务闭环。
- 确认工单、样品、检测项目、人员 ID、结果值和单位的正式字段。
- 确认 LIMS 成功/失败 JSON、幂等规则、鉴权方式及网络边界。

### 阶段 B：可控试运行

- 接入正式 LIMS 地址和认证。
- 增加结构化日志、请求 ID、推送记录、失败原因和人工重推入口。
- 设置数据库备份、磁盘告警、反向代理 HTTPS 和受限网络访问。
- 为正常、重复、非法样品、超时、LIMS 5xx 等场景建立自动化接口测试。

### 阶段 C：生产化

- 将 SQLite 迁移至 PostgreSQL，使用数据库迁移脚本管理表结构。
- 将回推改为持久化任务/Outbox 模式：先在本地事务内保存待推送任务，再由后台工作者重试，避免 HTTP 调用阻塞业务接口。
- 增加死信/人工处置队列、告警和端到端追踪。
- 按需要增加 LIMS 增量拉取调度器、角色权限、结果修订版本、数据报表和高可用部署。

## 13. 对外联调确认清单

在替换 Mock 接口前，应与 LIMS 和 SCADA 逐项确认：

1. `order_no` 是否全局唯一，是否可重用或撤销。
2. 实验人员、样品、检测项目的标准编码及最大长度。
3. 一个工单的样品数、项目数、单次结果上传数量上限。
4. `value` 的类型、精度、科学计数法、空值、检出限和合格判定表示法。
5. 单位是否使用统一字典，是否允许空单位或自由文本。
6. SCADA 获取方式是轮询、回调还是消息订阅；轮询间隔和重复获取语义是什么。
7. `finished` 的权威性和是否需要按样品/项目自动校验完整性。
8. LIMS 回推 URL、请求头、鉴权、超时、成功码、错误码和响应体格式。
9. 幂等键的作用范围、保存期限、重复请求返回格式与冲突策略。
10. 网络区域、代理、防火墙、证书、时钟同步和故障联系人。

## 14. 结论

当前架构以最少组件验证了 LIMS—服务—SCADA 的核心数据闭环：三张业务表、明确接口、基础事务、结果 UPSERT 和回推幂等键已经构成可联调的骨架。它的价值在于先把外部协议和业务规则固定下来；当协议稳定、并发和可靠性要求上升时，再按“安全、可观测、异步可靠投递、服务型数据库”的顺序演进，能避免在需求尚未明确时过早引入复杂基础设施。
