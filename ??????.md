# LIMS / SCADA 设备数据对接项目架构分析

## 1. 项目定位

项目是一个面向接口联调的 LIMS / SCADA 中转服务，将 LIMS 工单、SCADA 实验结果、设备检测客户端通知和 LIMS 结果回推放在同一条可运行链路中。网页控制台用于现场演示和人工核验，不改变后端业务边界。

## 2. 总体链路

```text
LIMS
  │ POST /api/work-orders
  ▼
FastAPI 中转服务 ─────── Web 联调控制台 /
  │                         │
  │ GET /api/scada/work-orders
  ▼                         │ POST /startTest、/stopTest
SCADA ── POST /api/scada/results
  │                         │
  └──────────► SQLite ◄─────┘
                  │
                  │ POST /api/work-orders/{order_no}/push-to-lims
                  ▼
             LIMS 或本地 Mock LIMS
```

网页通过相对路径调用同一个 FastAPI 服务，避免跨域和额外前端服务配置。页面把接口响应、检测状态和操作时间集中展示，适合对接过程中的快速确认。

## 3. 代码分层

```text
main.py
  └─ app.main.create_app
       ├─ api.routes       HTTP 路由
       ├─ schemas          Pydantic 请求与响应模型
       ├─ services         工单和检测会话业务规则
       ├─ repositories      SQLite 查询和写入
       ├─ db               连接管理与增量建表
       ├─ integrations      LIMS 客户端、录屏/截图适配器
       └─ core              环境配置与 UTC 时间工具

frontend/
  ├─ index.html            联调页面结构
  ├─ styles.css            视觉样式和响应式布局
  └─ app.js                API 调用、响应展示和事件日志
```

根目录 `main.py` 只导出 `app`，保留 `python -m uvicorn main:app --reload` 兼容启动方式。`app.main` 负责应用创建、依赖装配、数据库初始化和静态页面挂载。

## 4. 数据模型

### 4.1 `work_orders`

以 `order_no` 为唯一业务键，保存实验人员、项目名称、状态和原始 JSON。状态为：

```text
pending → in_progress → completed → pushed
```

### 4.2 `samples`

样品属于某一张工单，`(work_order_id, sample_no)` 唯一。结果上传前会校验样品是否属于目标工单。

### 4.3 `test_results`

以 `(work_order_id, sample_no, test_item)` 作为唯一键。重复上传会更新结果和单位，并把 `pushed_to_lims` 重置为 0，避免新值被误认为已经回推。

### 4.4 `detection_sessions`

保存设备检测会话：

| 字段 | 作用 |
| --- | --- |
| `blind_sample_no` | 设备接口传入的盲样号 |
| `sample_id` | 可选匹配的现有样品 ID |
| `status` | `recording`、`stopped` 或 `failed` |
| `started_at` / `ended_at` | 服务端 UTC ISO 8601 时间 |
| `recording_ref` / `screenshot_ref` | 采集适配器返回的引用 |
| `capture_mode` | `mock` 或正式适配器名称 |
| `error_message` | 采集失败原因 |

盲样号不要求预先存在于 `samples`，因此设备可以独立发起检测；匹配不到样品不会阻断开始接口。

## 5. 设备接口设计

`POST /startTest` 和 `POST /stopTest` 使用相同请求体：

```json
{"blindSampleNo":"92fbbc8e88c949158fade880b8d69b27"}
```

响应固定为：

```json
{"code":"0010","response":null,"message":"成功"}
```

业务失败仍使用 HTTP 200 和 `code = "500"`，便于兼容对方接口文档。JSON 缺字段、空字段或额外字段由 FastAPI 使用 HTTP 422 返回。

状态规则：

- 活跃会话重复开始：直接成功，不重复调用采集器。
- 已停止会话重复结束：直接成功，不重复截图。
- 没有会话直接结束：返回 `code = "500"`。
- 开始采集失败：写入 `failed` 会话和错误原因。
- 结束采集失败：保留 `recording`，不写成功结束时间。

## 6. 前端托管与交互

FastAPI 在 API 路由之后挂载 `frontend/`：

```python
app.mount("/", StaticFiles(directory=frontend_dir, html=True), name="frontend")
```

根路径返回 `index.html`，`/styles.css` 和 `/app.js` 由同一服务提供。前端采用原生浏览器 API `fetch`，统一处理 HTTP 错误、JSON 响应、事件日志和流程节点状态。接口调用失败时不伪造成功状态，响应面板会保留错误详情。

页面的 Mock 采集提示是重要边界：当前默认适配器不会生成真实文件，界面只展示协议调用与数据库会话状态。

## 7. 事务和失败窗口

- 工单和样品在同一个 SQLite 事务中写入。
- 一次结果上传中的所有 UPSERT 在同一事务中提交。
- LIMS 网络调用在本地事务之外执行。
- 只有 LIMS HTTP 调用成功后才更新本地 `pushed` 状态。
- LIMS 已成功但本地状态更新失败时，存在重复回推窗口；幂等键可降低重复写入风险。
- 当前网页是人工联调工具，不负责后台重试和任务调度。

## 8. 生产化风险与建议

当前原型没有鉴权和 HTTPS，任何能访问端口的客户端都可能调用接口。正式部署至少应增加 API 鉴权、来源限制、HTTPS、敏感字段脱敏日志、数据库备份和磁盘空间监控。

SQLite 适合单机和低并发联调。多进程、高频结果上传或多个设备并行接入时，应迁移 PostgreSQL，并将 LIMS 回推改造成带状态记录、重试和告警的 Outbox/任务队列模式。

真实录屏适配器还需要处理 Windows 进程生命周期、文件落盘、权限、异常退出、磁盘清理和媒体引用的长期保存。

## 9. 验证策略

- 接口测试覆盖工单幂等、结果 UPSERT、查询、回推和设备会话幂等。
- 静态资源测试覆盖 `/`、CSS、JavaScript 和 OpenAPI 路径。
- 通过真实 Uvicorn 完成浏览器端到端演示。
- 使用 `compileall` 检查模块导入和语法，使用 `pip check` 检查依赖。
