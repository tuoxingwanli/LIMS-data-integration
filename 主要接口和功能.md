# 主要接口和功能说明

## 1. 服务能力

服务提供五类能力：

1. LIMS 工单接收与持久化。
2. SCADA 工单查询与实验结果上传。
3. 检测客户端开始/结束通知。
4. 实验结果查询和回推 LIMS。
5. 浏览器网页联调和响应记录。

## 2. 接口清单

| 方法 | 路径 | 调用方 | 说明 |
| --- | --- | --- | --- |
| GET | `/health` | 系统 | 健康检查 |
| POST | `/api/work-orders` | LIMS | 接收工单 |
| GET | `/api/scada/work-orders?experimenter_id=EMP001` | SCADA | 获取待处理工单 |
| POST | `/api/scada/results` | SCADA | 上传或更新实验结果 |
| POST | `/startTest` | 检测客户端 | 开始检测通知 |
| POST | `/stopTest` | 检测客户端 | 结束检测通知 |
| GET | `/api/work-orders/{order_no}/results` | 联调 | 查询结果 |
| POST | `/api/work-orders/{order_no}/push-to-lims` | 服务/联调 | 回推结果 |
| POST | `/mock/lims/results` | 本地 Mock | 模拟 LIMS 接收 |

## 3. 网页联调控制台

访问 <http://127.0.0.1:8000/> 可打开网页。页面包括：

- 服务健康状态
- 本地联调与 Mock 采集状态提示
- LIMS 工单创建表单
- SCADA 工单查询
- 设备检测开始/结束按钮
- 实验结果上传、查询和 LIMS 回推
- 最近一次 API 响应 JSON
- 接口操作日志
- 工单、检测、结果、回推四步流程状态

网页只调用相对路径，因此和 FastAPI 服务使用同一端口，不需要 CORS 或 Node.js 开发服务器。响应区会展示原始 JSON，并将 HTTP 错误和 `code = "500"` 业务失败分别标记。

## 4. 检测客户端接口

### 4.1 开始检测

```text
POST /startTest
Content-Type: application/json
```

请求：

```json
{
  "blindSampleNo": "92fbbc8e88c949158fade880b8d69b27"
}
```

字段约束：

| 字段 | 必填 | 类型 | 说明 |
| --- | --- | --- | --- |
| `blindSampleNo` | 是 | string | 盲样编号，不能为空 |

服务调用采集适配器并记录 UTC 开始时间，成功后创建 `recording` 会话。

### 4.2 结束检测

```text
POST /stopTest
Content-Type: application/json
```

请求体和 `/startTest` 相同。服务找到活跃会话后调用结束采集，成功时记录结束时间和截图引用，并把会话更新为 `stopped`。

### 4.3 响应约定

成功：

```json
{
  "code": "0010",
  "response": null,
  "message": "成功"
}
```

业务失败：

```json
{
  "code": "500",
  "response": null,
  "message": "未找到正在进行的检测"
}
```

成功和业务失败均返回 HTTP 200。缺少字段、字段为空或包含额外字段时返回 HTTP 422。

### 4.4 幂等和状态

- 同一盲样号重复开始：返回成功，不重复创建会话或启动采集。
- 已结束会话重复结束：返回成功，不重复截图。
- 未找到会话直接结束：返回 `code = "500"`。
- 盲样号可以独立创建会话。
- 如果盲样号匹配 `samples.sample_no`，会保存可选样品关联。
- 默认 Mock 采集器成功但不生成真实视频或截图。

## 5. LIMS 工单接口

### 5.1 工单下发

```text
POST /api/work-orders
```

```json
{
  "order_no": "WO-DEMO-001",
  "experimenter_id": "EMP001",
  "project_name": "设备联调演示",
  "samples": [
    {"sample_no": "S-DEMO-001", "sample_name": "联调样品"}
  ],
  "test_items": ["Fe", "C", "Mn"]
}
```

相同 `order_no` 重复下发时返回成功和 `duplicate = true`，不会创建重复工单。

### 5.2 SCADA 获取工单

```text
GET /api/scada/work-orders?experimenter_id=EMP001
```

返回指定实验人员的 `pending` 和 `in_progress` 工单，并包含样品和检测项目。

### 5.3 上传实验结果

```text
POST /api/scada/results
```

```json
{
  "order_no": "WO-DEMO-001",
  "results": [
    {"sample_no": "S-DEMO-001", "test_item": "Fe", "value": 96.2, "unit": "%"}
  ],
  "finished": true
}
```

结果按工单、样品和检测项目 UPSERT。`finished = false` 时工单状态为 `in_progress`，`true` 时为 `completed`。

### 5.4 查询结果

```text
GET /api/work-orders/WO-DEMO-001/results
```

返回工单状态和结果列表。结果包含 `pushed_to_lims` 标记。

### 5.5 回推 LIMS

```text
POST /api/work-orders/WO-DEMO-001/push-to-lims
```

服务向 `LIMS_RESULT_URL` 发起 POST，并携带：

```text
X-Idempotency-Key: WO-DEMO-001
```

只有对方请求成功后，本地结果才标记为已回推，工单才进入 `pushed`。

## 6. 数据表

| 表 | 作用 |
| --- | --- |
| `work_orders` | 工单、实验人员和工单状态 |
| `samples` | 工单样品 |
| `test_results` | 实验结果和回推标记 |
| `detection_sessions` | 盲样检测会话、时间、状态和采集引用 |

## 7. 配置

```powershell
$env:LIMS_DB_PATH = "lims_demo.db"
$env:LIMS_RESULT_URL = "http://127.0.0.1:8000/mock/lims/results"
$env:CAPTURE_BACKEND = "mock"
python -m uvicorn main:app --reload
```

正式接入前应确认 LIMS 地址、鉴权、HTTPS、超时、重试、盲样号规则和真实采集程序。

## 8. 生产边界

当前版本是联调原型，不包含鉴权、HTTPS、真实录屏、截图文件管理、消息队列和自动重试。Mock 适配器不会创建媒体文件，正式采集功能需要替换 `app/integrations/capture.py` 中的适配器并补充进程、文件和权限管理。
