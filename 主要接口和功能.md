# 主要接口和功能说明

## 1. 服务功能

本服务提供四类能力：

1. LIMS 工单接收和持久化。
2. SCADA 工单查询和实验结果上传。
3. 检测客户端录屏开始/结束通知。
4. 实验结果回推 LIMS。

## 2. 接口清单

| 方法 | 路径 | 调用方 | 说明 |
| --- | --- | --- | --- |
| GET | `/health` | 系统 | 健康检查 |
| POST | `/api/work-orders` | LIMS | 下发工单 |
| GET | `/api/scada/work-orders` | SCADA | 按实验人员查询工单 |
| POST | `/api/scada/results` | SCADA | 上传实验结果 |
| POST | `/startTest` | 检测客户端 | 开始检测通知 |
| POST | `/stopTest` | 检测客户端 | 结束检测通知 |
| GET | `/api/work-orders/{order_no}/results` | 联调 | 查询结果 |
| POST | `/api/work-orders/{order_no}/push-to-lims` | 服务内部 | 回推结果 |

## 3. 检测客户端接口

### 3.1 开始检测通知

接口地址：

```text
POST http://IP:PORT/startTest
```

请求：

```json
{
  "blindSampleNo": "92fbbc8e88c949158fade880b8d69b27"
}
```

字段说明：

| 字段 | 必须 | 类型 | 说明 |
| --- | --- | --- | --- |
| `blindSampleNo` | 是 | string | 盲样编号，不能为空 |

处理结果：服务调用开始采集适配器并记录 UTC 开始时间，创建 `recording` 检测会话。

### 3.2 结束检测通知

接口地址：

```text
POST http://IP:PORT/stopTest
```

请求体与 `/startTest` 相同：

```json
{
  "blindSampleNo": "92fbbc8e88c949158fade880b8d69b27"
}
```

处理结果：服务调用结束采集适配器，执行截图动作并记录 UTC 结束时间，将会话更新为 `stopped`。

### 3.3 响应码

| code | 含义 | HTTP 状态 |
| --- | --- | --- |
| `0010` | 成功 | 200 |
| `500` | 业务失败 | 200 |

成功示例：

```json
{
  "code": "0010",
  "response": null,
  "message": "成功"
}
```

失败示例：

```json
{
  "code": "500",
  "response": null,
  "message": "未找到正在进行的检测"
}
```

请求 JSON 不符合字段约束时返回 HTTP 422，这是 FastAPI 的协议校验响应，不属于业务 `code`。

## 4. 检测会话规则

- 活动会话重复开始：返回成功，不重复录屏。
- 已停止会话重复结束：返回成功，不重复截图。
- 没有会话直接结束：返回 `code = "500"`。
- 盲样号可以独立创建会话。
- 如果盲样号等于已有 `samples.sample_no`，数据库会保存对应样品关联。
- 当前默认 Mock 采集器不创建真实视频和截图文件。

## 5. LIMS 工单接口

### 5.1 下发工单

```text
POST /api/work-orders
```

```json
{
  "order_no": "WO20260917001",
  "experimenter_id": "EMP001",
  "project_name": "钢材成分检测",
  "samples": [
    {"sample_no": "S001", "sample_name": "钢材样品1"}
  ],
  "test_items": ["Fe", "C", "Mn"]
}
```

同一 `order_no` 重复下发时返回成功和 `duplicate = true`，不会创建重复工单。

### 5.2 查询 SCADA 工单

```text
GET /api/scada/work-orders?experimenter_id=EMP001
```

返回 `pending` 和 `in_progress` 状态的工单，并包含样品和检测项目。

### 5.3 上传实验结果

```text
POST /api/scada/results
```

```json
{
  "order_no": "WO20260917001",
  "results": [
    {"sample_no": "S001", "test_item": "Fe", "value": 96.2, "unit": "%"}
  ],
  "finished": true
}
```

相同工单、样品和检测项目再次上传时更新原结果。`finished = false` 时工单保持 `in_progress`，为 `true` 时进入 `completed`。

### 5.4 查询结果

```text
GET /api/work-orders/WO20260917001/results
```

### 5.5 回推 LIMS

```text
POST /api/work-orders/WO20260917001/push-to-lims
```

服务向 `LIMS_RESULT_URL` 发起 POST，并携带：

```text
X-Idempotency-Key: WO20260917001
```

只有对方 HTTP 请求成功后，本地结果才标记为已推送，工单才变为 `pushed`。

## 6. 数据表

| 表 | 作用 |
| --- | --- |
| `work_orders` | 工单和状态 |
| `samples` | 工单样品 |
| `test_results` | 实验检测结果 |
| `detection_sessions` | 盲样检测会话、时间和采集引用 |

## 7. 配置项

```powershell
$env:LIMS_DB_PATH = "lims_demo.db"
$env:LIMS_RESULT_URL = "http://127.0.0.1:8000/mock/lims/results"
$env:CAPTURE_BACKEND = "mock"
python -m uvicorn main:app --reload
```

真实录屏、鉴权、HTTPS、重试、审计和生产数据库需要在双方联调确认后继续扩展。
